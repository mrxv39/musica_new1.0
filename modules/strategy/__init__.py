"""Strategy / decision helpers.

This package contains pure selection logic to:
- Find the unique matching `sub_strategies` row in SQLite
- Decide the move (and bet min/max) for a given hero hand
"""
